# Agent OS Standards Index

backend:
  api:
    description: Needs description - run /index-standards
  migrations:
    description: Needs description - run /index-standards
  models:
    description: Needs description - run /index-standards
  queries:
    description: Needs description - run /index-standards

frontend:
  accessibility:
    description: Needs description - run /index-standards
  components:
    description: Needs description - run /index-standards
  css:
    description: Needs description - run /index-standards
  declarative-ui-states:
    description: Using shared components like LoadingState and ErrorState for consistent UI.
  hooks-for-logic:
    description: Separating component logic into reusable, testable custom hooks.
  responsive:
    description: Needs description - run /index-standards
  standardized-components:
    description: How and why to use shared React components like Button and ExpandableCardButton.

global:
  coding-style:
    description: Needs description - run /index-standards
  commenting:
    description: Needs description - run /index-standards
  conventions:
    description: Needs description - run /index-standards
  error-handling:
    description: Needs description - run /index-standards
  legacy-manager-pattern:
    description: Guide for maintaining the old 'Manager/Controller' class-based architecture.
  tech-stack:
    description: Needs description - run /index-standards
  validation:
    description: Needs description - run /index-standards

testing:
  test-writing:
    description: Needs description - run /index-standards

